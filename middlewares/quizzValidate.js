const {check} = require('express-validator');
module.exports = function validateQuizz() {
    return [
        // check('title', 'Title is required').not().isEmpty(),
        // check('description', 'Description is required').not().isEmpty(),
        // check('thumbnail', 'Thumbnail is required').not().isEmpty(),
        // check('questions', 'Questions is required').not().isEmpty(),
        // check('questions.question', 'Question is required').not().isEmpty(),
        // check('questions.answers', 'Answers is required').not().isEmpty(),
        // check('questions.answers.answer', 'Answer is required').not().isEmpty(),   
        
    ]

}